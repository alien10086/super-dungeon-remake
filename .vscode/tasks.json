{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": ["build"],
            "group": "build",
            "options": {
                "cwd": "${workspaceFolder}/super-dungeon-remake"
            }
        }
    ]
}